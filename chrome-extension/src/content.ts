/**
 * content.ts
 * 
 * This is the main content script injected into web pages.
 * It will listen for paste events, extract clipboard content,
 * apply redaction using the core logic, and replace the pasted data.
 */